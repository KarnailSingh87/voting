# Render Blueprint – single Web Service hosting backend + both SPAs
# Docs: https://docs.render.com/blueprint-spec

services:
  - type: web
    name: securevote
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run render:build
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: ADMIN_URL
        sync: false
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        sync: false
    healthCheckPath: /health
